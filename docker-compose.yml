services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: twdfx
      POSTGRES_PASSWORD: twdfx_dev_password
      POSTGRES_DB: twdfx
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
